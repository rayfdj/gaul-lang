// Testing fib(40) to see how fast the interpreter can go
// ~80s user time in release mode (tree-walking interpreter baseline)
fn fib(n) { if(n <= 1) { n } else { fib(n - 1) + fib(n - 2) } }
println(fib(40))
